# Django_Api_template
Template to my personal Django projects.

## Docker and actions configuration
The first think to you need to do is configure the github actions and connect with docker, you ned to configure the token in Docker Hub to your repository connect with docker in the token.
